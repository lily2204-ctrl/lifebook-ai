# מסמך אפיון עבודה — לייףבוק (Lifebook)

**מסמך זה מחייב. Claude Code קורא אותו במלואו בתחילת כל סשן עבודה, לפני כל שינוי קוד.**
עדכון אחרון: 21 ביולי 2026 · בעלים: לילי

---

## 1. עקרונות ברזל — אין חריגות

1. **לא נוגעים בשום דבר שעובד.** כל שינוי בקוד קיים — רק באישור מפורש של הבעלים, ורק אחרי שהוצגו לה כל הסיכונים לפגיעה בפונקציונליות קיימת.
2. **אזורים אסורים למגע ללא אישור נפרד וכתוב:**
   - צינור עקביות הדמות (character consistency) — התמונה שהועלתה של הילד היא מקור הזהות היחיד בכל האיורים. עיקרון מוצר עליון, לעולם לא מתפשרים עליו.
   - הצינור הדיגיטלי (גנרציה, PDF דיגיטלי, זרימת האשף)
   - וובהוק התשלום של שופיפיי
   - זרימת האדמין ומערכת התבניות
3. **כל פיצ'ר חדש — בענף Git נפרד**, עם נקודת שחזור (tag) לפני התחלה. מיזוג ל־main רק באישור הבעלים.
4. **לפני כתיבת קוד:** דיווח תוכנית קצרה — אילו קבצים ייווצרו/ישתנו, מה הסיכונים, מה העלות (קריאות AI). המתנה לאישור.
5. **פיילוט לפני ריצה מלאה:** כל תהליך שעולה כסף (קריאות AI, הדפסה) נבדק קודם בגרסה מוקטנת וזולה.
6. **תוצרי ביניים נשמרים תמיד** (תיקיית debug) — כדי שכשל יאובחן בעיניים, בלי לשרוף ריצה נוספת.
7. **גנרציית ספר ללקוח חייבת להסתיים בדקות** — לא Batch API ולא תהליכים של שעות במסלול הלקוח.
8. **לעולם לא `git add -A` / `git add .` בפרויקט הזה.** קומיטים תמיד עם רשימת קבצים מפורשת — כדי שקבצי הזרימה החיה, נכסים שנמחקו או תוצרי debug לעולם לא ייסחפו לקומיט בטעות ומשם לפרודקשן (main = פריסה אוטומטית).
9. **אסור קבועי סגנון מקודדים בערוץ ההדפסה.** סגנון האיור נקרא **תמיד** מרשומת הספר (`illustration_style`) וממופה למחרוזת סגנון נאמנה. המיפוי חייב לכסות אחד-לאחד כל סגנון שהאשף מציע. ערך לא מוכר נופל למחרוזת גנרית שמכבדת את שם הסגנון כלשונו — **לעולם לא כופה סגנון אחר**. (באג היסטורי: ערוץ ההדפסה הזריק "Soft Storybook" אקוורל לכל ספר והתעלם מהשדה — שורש ריצת האקוורל של מילאן וסתירת הסיכום אצל ריי; נסגר 21.7.2026.)

---

## 2. ארכיטקטורת המוצר — שני מסלולי יצירה, שני פורמטי מסירה

### מסלולי יצירה (איך נולד ספר)
| מסלול | טריגר | תיאור |
|---|---|---|
| **א. אשף (Wizard)** | הלקוח באתר | ספר בהתאמה מלאה: העלאת תמונה → אשף → גנרציה → תשלום בשופיפיי |
| **ב. תבניות (Admin)** | הזמנה בחנות שופיפיי | הלקוח מזמין מוצר תבנית ומעלה תמונות דרך האפליקציה בחנות; הבעלים מייצרת את הספר באדמין |

### פורמטי מסירה (מה מקבל הלקוח)
| פורמט | קובץ | יעד |
|---|---|---|
| **דיגיטלי** | PDF דיגיטלי (הקיים) | הורדה ללקוח |
| **מודפס** | PDF דפוס לפי מפרט בוקפוד | נשלח לבוקפוד ב־API **וגם** ניתן ללקוח להורדה — אותו קובץ בדיוק, ללא שינוי מידות |

**כלל מפתח: כל ספר, מכל מסלול יצירה, יכול לצאת בכל פורמט מסירה.** מודול הדפוס מקבל bookId ומפיק קובץ — הוא לא יודע ולא צריך לדעת אם הספר נוצר באשף או באדמין. אסור לבנות צינור דפוס נפרד לתבניות.

### מטריצת הזרימות
- אשף + דיגיטלי → הזרימה הקיימת. **לא נוגעים.**
- אשף + מודפס → אחרי תשלום על מוצר "ספר מודפס": גנרציה → PDF דפוס → הזמנה בבוקפוד + קישור הורדה ללקוח.
- תבנית + דיגיטלי → הזרימה הקיימת באדמין. **לא נוגעים.**
- תבנית + מודפס → הבעלים מייצרת באדמין כרגיל; באדמין נוסף כפתור "הפק גרסת דפוס + שלח לבוקפוד" לספר מוכן.

---

## 3. מפרט PDF דפוס — לפי דרישות בוקפוד (bookpod.co.il)

### קובץ תוכן
- עמודים בודדים ברצף (לא כפולות מודבקות), פורמט PDF בלבד.
- גודל גזירה: **19×28.5 ס"מ** (יחס **2:3 מדויק**, פורמט עומד). גלישה (bleed): **3.2 מ"מ בדיוק** מכל צד → עמוד בפועל **19.64×29.14 ס"מ** (2320×3442px ב-300DPI).
  - **מידה זו מחליפה את 22×22 (הכרעת בעלים 2026-08-24).** אושר מול בוקפוד: מדפיסים כל מידה בין A6 ל-A4 בפורמט עומד, ואומת במחשבון המחירים שלהם — **אותה עלות בדיוק** כמו הריבוע (25 ₪ ל-28 עמ'), כריכה רכה, אותו נייר.
  - **הסיבה:** האיור נוצר 1024×1536 — 2:3 בדיוק. עמוד בפורמט 2:3 בולע את האיור כמעט שלם: החיתוך האפקטיבי (רק בגלל ה-bleed) הוא **1.09%** לעומת **33%** שהריבוע חייב. הריבוע היה מקור כל הכאב בטראק הדפוס; המידה החדשה מייתרת אותו.
- רזולוציה 300DPI. קובץ שטוח: בלי שכבות, בלי סימני חיתוך, פונטים סגורים (אצלנו הטקסט מוטמע כתמונה — אין בעיית פונטים).
- **בלי מספור עמודים ובלי שום אלמנט של הצופה הדיגיטלי.**
- כל רקע/איור שמגיע לקצה העמוד חייב לגלוש עד קצה ה־bleed.
- מספר עמודים מתחלק ב־4. המבנה: **28 עמודים בדיוק** —
  1. "הספר הזה שייך ל___"
  2. עמוד הקדשה
  3–26. שתים־עשרה כפולות סיפור
  27. עמוד סיום
  28. עמוד לוגו
- **כיוון עברי:** הדף הראשון בצד שמאל בפתיחת הספר. שוליים פנימיים (צד השדרה) רחבים יותר: 10 מ"מ פנימי, 6 מ"מ חיצוני לטקסט.

### כפולת סיפור — "פריסת אריאל" (הפריסה המחייבת, אושרה בבוקפוד)

**מבנה עמודים (מעודכן 19.7.2026):**
עמוד 1 הקדשה + עמוד 2 קישוטי + עמודים 3–(N×2+2) כפולות + עמודי כוכבים דינמיים (0–3, בסוף בלבד) + עמוד סיום + עמוד גב.
- כיוון RTL בוקפוד: אי-זוגי = **שמאל** בפתיחת הספר. עמוד האיור **אי-זוגי (שמאל)**, עמוד הטקסט **זוגי (ימין)**.
- עמודי כוכבים: בסוף בלבד — **לא** בעמוד 2.

**עמוד האיור (שמאל/אי-זוגי):** `illustrationJpeg` — **האיור הדיגיטלי כולו** (1024×1536), מוגדל למילוי עמוד 2:3 (`{ width: PAGE_W_PT, height: PAGE_H_PT }`), full-bleed, **אפס עיוות, אפס חיתוך תוכן**. לעולם לא חיתוך שמאבד ראשים, כתרים או פרטים.

**עמוד הטקסט (ימין/זוגי):** `textBgJpeg` — רקע וינייט רדיאלי בהיר הנבנה מהצבע הדומיננטי של האיור + overlay שקוף (PNG) של הטקסט העברי RTL, באותו יחס 2:3, ללא עיוות.

**רקע הטקסט = גרדיאנט גוונים בלבד (החלטה עיצובית סופית 2026-07-21):** `textBgJpeg` הוא וינייט רדיאלי — הצבע הדומיננטי של צד הטקסט + הבהרה רדיאלית במרכז. זו **החלטה עיצובית סופית: גוונים בלבד, לא המשך סצנה / outpaint**. אין לנסות "להמשיך את האיור" אל עמוד הטקסט — הטקסט יושב על גרדיאנט רך של גווני הסצנה בלבד.

**שבירת שורות הטקסט:** לפי פיסוק — שורה יורדת אחרי נקודה, פסיק, סימן קריאה או שאלה בנקודת הפסיק הקרובה ביותר שלא חורגת מרוחב הבלוק. גיבוי: word-wrap במקרה שאין פיסוק בתחום.

**צבע הטקסט:** חום המותג הכהה `#2c1a0e` תמיד — **בלי קו מתאר / הילה / צל מכל סוג**. הווינייטה בהירה במרכז שם יושב בלוק הטקסט, כך שהחום הכהה נקרא נקי ללא מתאר. (הוסר קו המתאר הלבן `rgba(255,255,255,0.6)` שהיה כאן.)

---

### פורמט עמוד איור — אופציה 2: שימוש חוזר בתמונת הספר הדיגיטלי (מעודכן 2026-08-24 לפורמט 2:3)

**עמוד האיור בדפוס = עמוד האיור של הספר הדיגיטלי עצמו לאותו עמוד** (`book.fullImages[i]`, פורטרט 1024×1536, הזהות כבר אושרה ע"י הלקוח). **אין יצירת AI חדשה בכלל בטראק הדפוס** — התמונה עוברת upscale ואז ממלאה את העמוד כולו. כך הספר המודפס זהה פיקסל-אחר-פיקסל לספר שהלקוח כבר ראה ואישר, בעלות יצירת-AI אפס. **בוטלו לגמרי**: היצירה-מחדש לכל עמוד, התמונה האופקית 1536×1024, ההיפוך האוטומטי, פיצול ה-binding, חיתוך ה-2:1, **וגם החיתוך הריבועי**.

**כללים מחייבים:**
1. **אין חיתוך תוכן כלל (2026-08-24)**: האיור 1024×1536 והעמוד 19.64×29.14 הם שניהם 2:3 — האיור נכנס שלם, `coverCropToPage` רק מגדיל אותו למילוי העמוד. החיתוך האפקטיבי **1.09%** ונובע מה-bleed בלבד, לא מהפורמט. `smartSquareCropUp` ו-`coverCropToSquare` יצאו מהמסלול החי. אין מסגור/מַט/וינייט של האיור השלם — לא כאופציה ולא כגיבוי.
2. **זהות — לא נוגעים (אושרה)**: עצם השימוש החוזר בתמונה הדיגיטלית = זהות פיקסל-אחר-פיקסל. אין `images.edit`, אין לוקאלייק, אין דריפט.
3. **שער עיניים אנושי (הבעלים/הסוכן)**: לפני הצגת כל עמוד לבעלים — פותחים את עמוד האיור ומסתכלים בפועל: **"האם הדמות הראשית שלמה בפריים בלי קטיעה מקרית בקצה?"**. בריצה מלאה: כל 12 עוברות מבט ידני לפני שמגיעות לבעלים.

**עמוד טקסט = גוונים בלבד**: וינייטה מהצבע הדומיננטי של **כל האיור**, טקסט עברי בגופן **פרנק-רול ליברה** (Frank Ruhl Libre, נבחר ע"י הבעלים מרשת 5 גופנים), ממורכז אופקית, צבע `#2c1a0e` ללא מתאר/הילה/צל. ללא עלות AI. הגופן מוטמע כ-static instances (fonttools) תחת `print-pdf/fonts/` כי node-canvas מתעלם מציר המשקל בגופן משתנה.
**Upscaling**: האיור המלא עובר Replicate Real-ESRGAN ×4, ואז `coverCropToPage` לרזולוציית דפוס (2320×3442) — 2:3→2:3, אפס חיתוך תוכן, אפס מתיחה.
**עלות**: יצירת-AI $0.00 + upscaling בלבד ≈ $0.07/ספר.

### היסטוריה — חוזה "הריבוע כקנבס" והשליש התחתון (בוטלו 2026-08-24)
פרק זה נשמר כדי שלא ינוסה שוב. משהפורמט עבר ל-2:3, **שני הכללים האלה מתו יחד עם הסיבה שלהם**:
- **כלל שני-השלישים** (מ-2026-08-10): חייב את כל הדמויות והעלילה לשני השלישים העליונים, והשאיר את השליש התחתון קרקע ואווירה — הגנה מפני החיתוך הריבועי. **נמחק, ולא חוזר.** הוא נוצר אך ורק כהגנה מחיתוך שלא קיים יותר, והמחקר הראה שהוא מצמצם קומפוזיציות. הדמויות והסצנה חופשיות למלוא הפריים, כמו בספרים שלפני אוגוסט.
- **חוזה הריבוע-כקנבס** (2026-08-23): דרש שהריבוע העליון יחזיק סצנה שלמה עומדת בפני עצמה, והרצועה התחתונה תהיה המשך רגוע. **נמחק** מההנחיות ומהשערים (`scoreSquareCanvas`, `scoreBook` הוסרו מ-`composition-gate.js`).
- **הארכה דטרמיניסטית של הרצועה** (`square-extend.js`, שתי גרסאות: focus-ramp ו-perspective): **נפסלה בעיניים** — הראשונה יצרה לוח שטוח עם גבול חד, השנייה יצרה השתקפות מראה. שורש הסיבה נמדד: אין רצועת קרקע להמשיך, כי החוזה עצמו גורם למודל למלא את הריבוע מקצה לקצה (פירוט קצה תחתון 83–220% מממוצע התמונה ב-11 מ-12 עמודים).
- **מה נשאר מהמחקר, ובמלואו**: **עושר הסצנה, גיוון המסגורים ושער ההנחיות** — הם הלב של השיפור ואינם קשורים לפורמט.

### ניסוח פתיחת הסגנון — "הסצנה קודמת לדמות" (נפרס 2026-08-25)
`STYLE_LOCK.soft3d` נפתח בעבר ב־"Transform this into a 3D rendered animated character." — הוראה **להמיר את תמונת הייחוס לדמות**, שמשכה כמעט כל עמוד לקרופ פורטרט בלי קשר למסגור שהסיפור ביקש. הפתיחה הוחלפה בניסוח שמתחיל בסצנה: הסצנה, לא הילד, מכריעה את המסגור.
- **איך נמדד**: שני ספרי ניסיון מלאים (13 תמונות כל אחד) על ילד בדיוני עם רפרנס סינתטי — אותו סיפור, אותו רפרנס, אותו זנב, אותו מודל/גודל/איכות. **המשתנה היחיד היה משפט הפתיחה.**
- **תוצאה**: עמידה במסגור המבוקש **8/12 → 9/12**. עמודים 3, 4, 10 נפתחו; עמוד 6 התהדק. **עקביות הזהות לא זזה כלל** — נבדקה על כל 13 הפנים מול הרפרנס, ועמוד-מול-עמוד בין הספרים בזום פנים.
- **עלות המחקר**: $6.87 (ספר ניסיון 2 $3.33 · שתי בדיקות בודדות $0.50 · ספר ניסיון 3 $3.04).
- **מה הופרך בדרך**: ההשערה שמשפט "ראש, שיער וכתפיים גלויים" הוא שכובל את המסגור — **הופרכה** בבדיקה מבוקרת (עם/בלי, כמעט זהות). המשפט נשאר.
- **אזהרה מתודית**: הבדיקה הבודדת הראשונה נחתה על עמוד 3 והבטיחה פריצה גדולה בהרבה ממה שהספר המלא נתן. **תמונה בודדת אינה מדגם** — אין להסיק מסקנת מסגור בלי ספר מלא מול ביקורת.

### כרטיס מחקר פתוח — עמודי wide שלא נפתחים (נפתח 2026-08-25, לאחר ההשקה)
**הבעיה**: גם אחרי ניסוח הפתיחה החדש, 3 מ-12 עמודים לא מממשים את המסגור שהסופר ביקש, ו**עמודי ה-wide הם הקשים**: עמודים 0 ו-11 ביקשו wide ונשארו תקריב/medium **בשני הספרים** — כלומר יש כאן גורם שהניסוח לא נוגע בו.
**החשוד הראשי**: **תמונת הייחוס עצמה** — פורטרט ראש-וכתפיים מושך את המודל לפורטרט. `images.edit` מקבל את הרפרנס כתמונת מקור, וסביר שהמסגור שלו דולף לתוצאה.
**כיווני בדיקה** (לא נבדקו, לא מאושרים): רפרנס בגוף מלא · רפרנס על קנבס 2:3 במקום 1:1 · הפרדת זהות ממסגור בין שתי קריאות.
**עדיפות**: אחרי ההשקה. לא חוסם — 9/12 עם זהות מושלמת טוב משמעותית מנקודת המוצא.
**מלכודת מדידה מתועדת**: מדד "מסת הדמות" האוטומטי (ספירת פיקסלים בגוון עור) **פגום ואין להסתמך עליו** — הוא סופר חיות חומות (ארנבים, שועל, דוב) כדמות וקרא 29.5% לתמונה שהמסגור בה נפתח בבירור. שיפוט המסגור נעשה בעין, על גיליונות באותה רזולוציה לשני הצדדים.

### שערי איכות (שלב 1, נסגר 21.7.2026 — עודכן 2026-08-24 לפורמט 2:3)
1. **אפס חיתוך תוכן**: כמפורט בכלל 1 לעיל. אין יצירה-מחדש, אין מסגור, אין חיתוך ריבועי.
2. **גאומטריה דטרמיניסטית יחידה** (`computeSpreadGeometry`): כל התמונה היא עמוד האיור (`splitX = W`, `frameValid = true`, בלי היפוך/פיצול/חיתוך). מקור-אמת יחיד לשלב ה-upscale ולהרכבה.
3. **שער ההנחיות** (`scoreImagePrompts` ב-`composition-gate.js`): לפני שמשלמים על תמונה כלשהי, בודק שכל imagePrompt נפתח במסגור מוצהר, שיש לפחות 3 wide + 3 full-body ולא יותר מ-2 close-up, ושאין שני עמודים שהם אותה סצנה (חפיפה >60%). דטרמיניסטי, בחינם, **log-only — לא חוסם ולא משנה כלום**.
4. **שער עיניים אנושי = הקובע הסופי**: השער האוטומטי עיוור לבגד לבן על רקע בהיר, לכן מבחן העיניים של הבעלים/הסוכן הוא הקובע.

**איכות מובטחת (אוטומטית):**
- **אפס מתיחה** בכל שלב — כל שינוי-גודל שומר יחס (`coverCropToPage`, סקאלר יחיד).
- **רזולוציה אפקטיבית ≥2320px** ברוחב אחרי upscale (300DPI אמיתי לעמוד 19.64 ס"מ) — אחרת אזהרה.
- **סף חדות** (Laplacian variance) — תמונה רכה מדי מסומנת; כרגע log-only, מכויל על ריי לפני אכיפה קשה.
- **פיצוי דפוס**: הבהרה ~5% של האיור ורקע הטקסט בלבד (לעולם לא שכבת הטקסט הכהה) — כי דפוס תמיד כהה מהמסך.

### כלל: סיכום אימות לפני כל ריצה בתשלום
לפני כל ריצת דפוס שעולה כסף, המנוע מדפיס **סיכום אימות** (`printVerificationSummary`) ומחכה לאישור הבעלים: bookId הייחודי הארוך, שם הילד, כותרת, סגנון, מספר עמודים, ותמונת הרפרנס (נשלפת לפי ה-bookId בלבד ונשמרת ל-`reference-used.jpg`). מצב `dryRun:true` מדפיס את הסיכום ומדווח כמה עמודים ובכמה $ — בלי להוציא שקל.

### ניסיון פנורמי — שמור לשדרוג עתידי (tag: `panoramic-spread-experiment`)
ניסיון שנבדק ב-2026-07: קנבס 1536×1024, מקורי ימין ללא עיוות, הרחבה שמאלה, פיצול לשני עמודים. איכות ה-outpaint הייתה טובה (המשך סצנה אמין). הופסק כי הפריסה הנוכחית (אריאל) כבר מאושרת בבוקפוד ואין צורך בשינוי. ניתן לחזור אליו בעתיד.

### איכות ומשקל
- Upscaling לרזולוציית דפוס (Replicate / Real-ESRGAN) לפני הרכבה.
- הטמעת תמונות כ־JPEG באיכות 85–90. יעד משקל: מתחת ל־80MB.
- תוצרי ביניים (1:1, מורחבות, אחרי upscaling) נשמרים ב־`print-pdf/debug/`.

### קובץ כריכה — יריעה פרושה אחת (הוכרע 27.8.2026 מול המדריך הרשמי של בוקפוד)

**המבנה: דף אחד, `[ חזית | שדרה | גב ]` משמאל לימין.** המדריך: *"הכריכה צריכה להיות בדף אחד, הכולל את החלק הקדמי, השדרה (מכונה גם גב), והחלק האחורי."*

**כלל הכיוון (שורש ההיפוך אצל אריאל):** בספר עברי (RTL) השדרה יושבת מימין לכריכה הקדמית, ולכן **החזית היא הפאנל השמאלי**. אומת מול "דוגמה לכריכה תקינה" במדריך, שבה פאנל הכותרת שמאלה לשדרה ופאנל הטקסט+ברקוד מימין לה. המחולל הישן (`344aa31`) בנה `[גב | שדרה | חזית]` — סידור לועזי, וזו הייתה הסיבה להיפוך.

**גיאומטריה (28 עמודים):**
```
שדרה     (28 ÷ 2) × 0.16          =   2.24 מ"מ
TrimBox   190 + 2.24 + 190 × 285  = 382.24 × 285.00 מ"מ
MediaBox  382.24 + 2×3.2 × 291.4  = 388.64 × 291.40 מ"מ   (4591 × 3442 px @300DPI)
```
- הקונפיגורטור של בוקפוד מציג 0.225 ס"מ לספר הזה — פער 0.01 מ"מ מהחישוב, עיגול בלבד.
- **הנוסחה היא לפי גיליונות, לא לפי עמודים.** הניסוח במדריך ("עובי 0.1 מ"מ לעמוד") מטעה: לפי עמודים היינו מקבלים 4.48 מ"מ, כפול מהמספר של הקונפיגורטור שלהם.
- **הנייר: כרומו מט 170 גרם → 0.16 מ"מ.** זהו השילוב היחיד בטבלת הנייר שלהם שנוחת על 0.225 ס"מ ל-28 עמודים.
- **אסור לקודד את השדרה קשיח.** `computeSpineMM(pageCount, thicknessMM)`; `pageCount` מגיע מ-`generatePrintPDF` דרך `server.js`, לא מקבוע.

**הרכבה:** כל פאנל מצויר על קנבס בגודל עמוד מלא (bleed בארבעת הצדדים), ובהרכבה נגזרת ממנו רק **רצועת ה-bleed הפנימית** — זו שהשדרה מחליפה. שטח הגזירה המודפס של שני הפאנלים נשאר זהה לגרסת 2 העמודים (אומת: הפרש מקסימלי 7/255 בחזית, רעש JPEG בלבד).

**⚠️ סתירה במדריך — הוכרע לטובת 3.2 מ"מ.** תחת "עיצוב כריכה פנימית" מופיע *"יש להשאיר 20 מ"מ שוליים (גלישה) מכל ארבעת הצדדים עבור **עטיפת הקרטון**"* + ביג 8–10 מ"מ מכל צד של השדרה. הפסקה הזו מתארת **כריכה קשה**, ובוקפוד עצמם כותבים *"כל הכריכות אצלנו הם כריכה רכה בהדבקה"*. אנחנו עובדים לפי ההנחיה הכללית (סעיף 6): **גלישה של 3.2 מ"מ בדיוק מכל הצדדים**, ומסמנים את התיבה המתאימה בהעלאה. לאמת על ההוכחה הפיזית.

**ברקוד:** בדוגמה שלהם יש ברקוד על הגב; לנו אין. מהספר של אריאל ידוע שהברקוד באחריותנו ולא של בוקפוד. **להוכחה הפיזית ממשיכים בלי** (הכרעת בעלים 30.8.2026). כרטיס עתידי ב-CLAUDE.md: לברר אם ברקוד/ISBN נדרש בכלל למוצר מותאם-אישית שאינו נמכר בקמעונאות, ומה לשים שם.

**נכס הלוגו — חייב להשתלב ברקע.** `lifebook-logo-print-cream.png` אטום לחלוטין והקרם שלו צרוב ב-rgb(253,246,236), לא ב-`#fdf8f0` של העמודים. צויר כמות שהוא הוא מלבן קרם קר יותר עם קצה חד, בכל חמשת עמודי הלוגו (הקדשה, מילוי, סיום, גב הספר, גב הכריכה). `loadLogoPng` מזיזה את כל התמונה בהפרש בין שני הקרמים — הרקע נוחת על צבע העמוד בדיוק, דיו הלוגו זז ב-≤4/255, וקצוות ה-antialiasing נשמרים (מפתח אלפא לפי סף היה הורס אותם). **אם מחליפים את נכס הלוגו — לוודא מחדש.**

### תיבות עמוד (TrimBox / BleedBox) — חובה בכל קובץ דפוס
PDFKit כותב במילון העמוד **רק `/MediaBox`**. קובץ שמופק כך מצהיר על מידת היריעה אבל לא על מקום הגזירה — ולכן וולידטור מודד את היריעה כולה, כולל bleed, ומדווח 196.4×291.4 מול ציפייה של 190×285. זה בדיוק מה שבוקפוד החזירו ב-27.8.2026 (*"ה-TrimBox חסר... ההשוואה בוצעה לפי מידת העמוד הנראית"*), בשני הקבצים.
- `stampPageBoxes()` ב-`print-pdf-generator.cjs` מטביעה `TrimBox` (העמוד הגמור) + `BleedBox` + `CropBox`, ו**חייבת להיקרא מיד אחרי יצירת כל עמוד** — כולל העמוד ש-`autoFirstPage: true` יוצר לבד.
- אימות: `TrimBox` אחיד בכל העמודים ושווה בדיוק ל-190×285 (או למידת הגזירה של היריעה, בכריכה פרושה).
- אפס השפעה על הפיקסלים — זו הצהרה בלבד.

### בדיקות חובה לפני כל שליחה לדפוס
1. העלאה למערכת התצוגה המקדימה של בוקפוד — בדיקת קווי חיתוך אדומים, גודל מזוהה, מספר עמודים.
2. בדיקת הבעלים בזום 100%: חדות, תפרים בכפולות, טקסט קריא, זהות הילד נשמרת.
3. לפני מכירה ראשונה ללקוח: עותק בדיקה פיזי אחד לבעלים. אין ויתור.

---

## 4. PDF דיגיטלי — המצב הקיים

- הצינור הקיים (jsPDF בצד לקוח / delivery) נשאר כמות שהוא. **לא נוגעים.**
- לקוח שרכש ספר מודפס מקבל להורדה את **קובץ הדפוס עצמו** — לא נוצרת גרסה דיגיטלית שלישית.

---

## 5. אינטגרציית בוקפוד (API)

- מודל Pre-paid: יתרת קרדיטים בחשבון. אין יתרה — אין הזמנה. לפני כל הזמנה: בדיקת יתרה ודיווח לבעלים.
- יכולות בשימוש: Create Book (שיוך קבצי תוכן + כריכה) · Create Order (פרטי משלוח) · Shipment Tracking · Calculate Production Cost / Order Price.
- **לא משתמשים בתוסף שופיפיי של בוקפוד** — הוא בנוי לקבצי דפוס קבועים ברמת מוצר, ואצלנו כל הזמנה מייצרת קובץ ייחודי. החיבור: API ישיר מתוך המנוע.
- טוקן ה־API נשמר כמשתנה סביבה בלבד. לעולם לא בקוד ולא בריפו.
- כל שליחת הזמנה אמיתית לבוקפוד (= כסף) — באישור הבעלים, עד שהזרימה מאושרת כיציבה.

### מקור האמת ל-API (הוכרע 30.8.2026)

**התוסף הרשמי שלהם, לא המסמכים.** `BookPod Author Tools` v2.2.2 מ-wordpress.org הוא הקוד שבוקפוד עצמם כותבים מול ה-API שלהם. מסמכי ה-PDF שלהם מינואר 2025 מתארים זרימת העלאה **שכבר לא קיימת**, והקוד שלנו נבנה לפיהם — ולכן ההרצה החיה הראשונה (30.8.2026) נכשלה ב-`HTTP 500 {"success":false,"error":"Unexpected field"}`.

הסיבה מופיעה מילולית ב-`bookpod-author-tools.php:615`: *"BookPod backend expects `upload.fields([{ name: 'images' }])`"*. כלומר `POST /api/v1/books` מקבל **שדה קובץ אחד בלבד — `images`** (תמונות תצוגה לחנות). שני ה-PDF-ים שלנו הגיעו כ-`content_file`/`cover_file` ונדחו על ידי multer.

### יצירת ספר — שלושה שלבים

| שלב | קריאה | תוכן |
|---|---|---|
| 1 | `POST /api/v1/books/upload-url` | JSON `{contentFileName, coverFileName}` → מחזיר `contentUploadUrl`/`coverUploadUrl` — כתובות חתומות ל-Google Cloud Storage. **לא יוצר כלום ולא עולה כלום** — לכן זו בדיקת החיווט הבטוחה |
| 2 | `PUT` לכתובת החתומה | בייטים גולמיים, `Content-Type: application/pdf`, **בלי כותרות אימות** — החתימה בכתובת היא האישור, וכותרות משלנו ישברו אותה |
| 3 | `POST /api/v1/books` | JSON **שטוח** (לא תחת מפתח `data`) + `contentUrl`/`coverUrl` כ-`gs://bucket/object` → מחזיר `bookId` |

**מפרט הדפוס בשלב 3** — רק הערכים שבטופס שלהם (`bpat-book.php:948-990`) חוקיים: `printcolor=color` · `sheettype=chromo170` · `finishtype=soft` (האפשרות היחידה) · `readingdirection=right` · `width=19` `height=28.5` (ס"מ; מותר 12–22 ו-17–29.7) · `bleed=true`. **`laminationtype` נשאר פרמטר פתוח** (`none`/`flat`/`matt`), ברירת מחדל `matt`; יוכרע לפי תשובת בוקפוד וההוכחה הפיזית.

`sheettype=chromo170` הוא **אישור עצמאי לנייר** שכבר הוסק מהשדרה בדרך אחרת לגמרי (0.16 מ"מ לגיליון מול הקונפיגורטור שלהם).

### שתי הנחות שהיו שגויות ותוקנו

- **`status` הוא לא "טיוטה"** אלא *"Show in store?"* — הצגה בחנות שלהם בלבד. אין מצב טיוטה: הספר נוצר בחשבון בכל מקרה. עדיין **לא צורך קרדיטים** — רק הזמנה צורכת.
- **`external_id` לא קיים ב-API** — אפס מופעים בכל התוסף. העקיבות עוברת ל-`reference_num1` בתוך `shippingDetails` של ההזמנה.

### שם הספר בחשבון — `title` (הוכרע 6.9.2026)

**`title` הוא שדה השם היחיד בכל ה-API** (`bpat-book.php:1400`, שדה חובה בטופס שלהם). אין `bookname`, אין כותרת משנה, ואין שדה פנימי נסתר — כל מופע אחר של `name` בתוסף הוא נמען משלוח או שם קובץ. לכן המחרוזת הזו היא **הדרך היחידה להבחין בין ספרים של לקוחות שונים בדשבורד שלהם**.

הפורמט המחייב, נבנה ב-`buildBookpodTitle()` ב-`server.js`:

> `<כותרת הסיפור> · <שם משפחה> · <8 התווים הראשונים של ה-bookId>`
> לדוגמה: `ההרפתקה הקסומה של ריי · ינאי · 1b0bf073`

כותרת הסיפור כבר מכילה את שמו הפרטי של הילד, ולכן הוא לא משוכפל. שם המשפחה מגיע מפרטי המשלוח ומפריד בין שני ילדים באותו שם. קידומת המזהה קושרת שורה בדשבורד לכרטיס בתחנה. `description` נושא את מזהה ההזמנה לעקיבות — הוא לא מוצג ללקוח כי `status=false`.

**אין ב-API עריכה ואין מחיקה של ספר.** נקודות הקצה היחידות הן `books/upload-url` (POST), `books` (POST), `books/verify` (POST, קריאה בלבד — מחזירה `title`), ו-`books/sync-match` (POST, מתאים מוצרי ווקומרס לספרים קיימים **לפי כותרת** — לא משנה כותרת). אין `PUT`/`PATCH`/`DELETE`. **מכאן נובע כלל ברזל: ספר שנוצר בשם שגוי נשאר בחשבון לנצח.** לכן מזהה הספר נשמר ב-`print_orders.bookpod_book_id` **מיד אחרי `createBook` ולפני `createOrder`**, ולחיצה חוזרת על "אשר ושלח לדפוס" מדלגת על היצירה ועוברת ישר להזמנה. בלי זה, כישלון בהזמנה (דגל כבוי, תקלת רשת) מייצר ספר כפול בכל לחיצה. קרה פעם אחת ב-30.8.2026.

### הזמנה — `POST /api/v1/orders`

מבנה שונה לחלוטין ממה שהקוד הישן שלח (`bpat-order.php:44-50`, `:249-380`): `shippingDetails` עם `name`/`phoneNumber`/`zipCode`/`house`/`shippingMethod`/`shippingCompanyId`/`acceptTerms`, מערך `items` עם `{type:'book', bookid, quantity}`, ו-`totalprice`. התשובה: `{success, order_no}` — **ייתכן `success:false` בתוך HTTP 200**, אז קוד הסטטוס לבדו לא מספיק. אין `tracking_number` בתשובה; מעקב משלוחים הוא זרימה נפרדת.

הקוד יושב ב-`print-pdf/bookpod-order.cjs` — קובץ נפרד בכוונה — וחסום ב-`BOOKPOD_ORDERS_ENABLED`. בלי המשתנה הזה הפונקציה זורקת לפני שהיא מגיעה לבוקפוד.

---

## 6. עבודת התבניות — יישור קו (מסלול עבודה נפרד)

**מטרה:** התאמה מלאה בין התבניות המוצגות בחנות שופיפיי לבין התבניות הקיימות באדמין.

- שלב 1 — מיפוי: רשימת כל התבניות בחנות מול כל התבניות באדמין; טבלת פערים (קיימת בחנות וחסרה באדמין, ולהפך; פערי שמות/תוכן/סגנון).
- שלב 2 — החלטת הבעלים על רשימת התבניות הסופית.
- שלב 3 — ביצוע: השלמת החסר באדמין, עדכון מוצרי החנות. תבניות חדשות נבנות לפי אותם עקרונות מוצר (זהות הילד מהתמונה, סגנונות האיור הקיימים).
- כל תבנית חדשה תומכת בשני פורמטי המסירה מיום הקמתה.
- מסלול זה מתנהל בענף נפרד ולא מערבב שינויים עם מסלול הדפוס.

---

## 7. סטטוס נוכחי (21.7.2026)

- ✅ **שלב 1 נסגר ומוזג ל-main**: פורמט Wide Spread, ארבעת כללי הייצור, שלושת תיקוני המסגור, שערי איכות (אפס מתיחה, רזולוציה ≥2674px, חדות, פיצוי דפוס ~5%), וסיכום אימות + מצב dryRun. שינוי server.js הוגבל ל-endpoint ההדפסה בלבד (owner-only); אפס נגיעה בזרימה הדיגיטלית החיה. תג שחזור: `pre-print-pdf-merge-2026-07-21`.
- 🧊 ספר מילאן (`de8f527f-...`) **קפוא** — שירת כספר בדיקה, אין עליו עוד ריצות.
- 🔜 שלב 3 — ספר האימות **ריי ינאי** (דינוזאורים): סיכום אימות → אישור → כפולה אחת → אישור → ריצה מלאה. הריצה מפיקה **קובץ תוכן + קובץ כריכה** (כריכה = 1–2 עמודים בגודל trim עם bleed, לא פרוסה).
- 🔜 מחולל הכריכה: לעדכן לפורמט 1–2 עמודים (חזית+גב) לפי מסמך בוקפוד — החסם ירד.
- 🔜 שלב 4 — אם ריצת ריי נקייה: ההזמנה הראשונה דרך מודול בוקפוד (טריגר ידני).
- 🔜 במקביל (בעלים): טופס רישום API בבוקפוד, הפקדת קרדיטים.
- ✅ **פורמט הדפוס אושר מול בוקפוד (2026-08-24): 19×28.5 ס"מ, 2:3, כריכה רכה, אותו נייר, אותה עלות** (25 ₪ ל-28 עמ'). מחליף את 22×22.
